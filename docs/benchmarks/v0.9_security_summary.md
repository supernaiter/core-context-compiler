# v0.9 Security Summary

Trust tiers, quarantine, rollback metrics, poison acceptance, poison activation, and audit log artifacts are emitted by the security suite.

Known weak result: rollback is deterministic and local; distributed backend rollback is interface-only.
