# v0.3 Token Efficiency Summary

Token inversion failed.

- `compressed_core_plus_recall` total_tokens: `8294`
- `naive_rag` total_tokens: `589`
- `compressed_core_plus_recall` preserves accuracy/source/stale/abstention advantages.
- Best representation in the current deterministic sweep: `verbose`

Decision:

- Do not claim token efficiency superiority yet.
- Next task: reduce duplicated core/recalled memory and make source verification source-id-only when metrics allow it.
