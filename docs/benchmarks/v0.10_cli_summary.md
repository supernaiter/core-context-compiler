# v0.10 CLI Summary

The `corectx` command exposes ingest, compile, answer, eval, ablate, benchmark, report, inspect, and rollback.

Known weak result: inspect and rollback are smoke-test commands unless a persistent store is wired.
