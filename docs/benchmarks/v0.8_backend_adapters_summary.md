# v0.8 Backend Adapters Summary

In-memory and JSONL backends are CI-safe. pgvector, Graphiti, LangMem, and Mem0 adapters are interface-compatible mocks unless optional external services are wired by the user.
