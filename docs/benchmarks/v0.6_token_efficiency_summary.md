# v0.6 Token Efficiency Summary

Token attribution, recall overfire, representation efficiency, and score-per-1k-token reports are implemented.

Known weak result: absolute token inversion against naive RAG may fail on synthetic_v2; release gating allows score-per-1k-token improvement only when reported explicitly.
